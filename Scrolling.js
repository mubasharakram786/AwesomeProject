import React from 'react';
import { ScrollView,View, Text,Image  } from 'react-native-web';

const Scrolling=()=>{
    return(
        <ScrollView>
            <Text style={{fontSize:100}}>Scroll View</Text>
            
        </ScrollView>
    )
}
export default Scrolling;